# Die 朝向与层间接口

## 使用

- 在「架构输入 → 自定义 → Die 层与尺寸」设置各 Die 正面朝上、朝下或待定。表格从下往上排列。
- 在同一表格下方选择既有接口的 TSV / HB，统一点击「应用结构并预览」。修改保留区域坐标、尺寸、core 归属及信号预算；转换接口后须重新检查 pitch 和容量。新接口的预留区域与预算目前通过芯片架构 YAML 定义。
- 布局顶部从左到右显示顶层到底层。每张 Die 卡片的上下色带分别表示其上、下表面。点击卡片查看剖面，点击层间接口查看预算及区域明细。
- 「单层正面」显示模块和布线；「单层背面」显示硅基底与 TSV 落点，左右镜像，不透视显示另一面的模块。两种观察方式都不修改放置数据。编辑模块时自动使用正面视图。
- 点击模块、Die、连线或 TSV 展开右侧详情；点击画布空白或关闭按钮收起。约束检查默认折叠。

## 输入与兼容

下载的独立芯片架构 YAML 支持顶层 `stack`，工艺库仍仅包含 `resources`：

```yaml
schema_version: resim-architecture/1
stack:
  die_faces:
    xdie: down
    ldie: up
    bdie: down
    dram_8layers: unknown
# name / architecture / constraints / floorplan / search 与原格式一致
```

`die_faces` 仅允许存在的 Die ID，值为 `up`、`down`、`unknown`。旧输入未指定时从 F2F/B2B 接口推断；无法推断时显示待定，不当作已知朝上。抽象 DRAM 封装不假设内部 die 朝向。

当前发布包的计算引擎仍为 0.1，严格 schema 没有独立朝向字段。因此 Web 适配层把 `stack` 编码为 `architecture.description` 尾部的 `[resim-stack/1]` JSON 块，以保留在完整输入、历史结果和候选方案中；导出独立芯片文件时还原为 `stack` 并移除兼容块。原文字说明保留。后续升级引擎 schema 时应迁移为正式字段，不依赖说明字段长期承载结构数据。

## 几何和资源口径

接触关系按 **下层的上表面 → 上层的下表面** 计算：

| 下层正面 | 上层正面 | 接触关系 |
| --- | --- | --- |
| 朝上 | 朝下 | F2F |
| 朝下 | 朝上 | B2B |
| 朝上 | 朝上 | F2B |
| 朝下 | 朝下 | B2F |

当前直接 HB 模型支持 F2F：显示接点/界面说明，不画穿硅柱，不计独立占地预留。TSV 按原有区域、pitch、禁布间距和预算计算；显示穿硅路径和落点。显示厚度与展开间距为观察用途，不把屏幕高度带入资源计算。平面坐标沿用工程全局坐标，朝向切换不镜像或重排已放置模块。

HB 与 TSV 是不同的互联机制，不是物理上互斥的两种封装。背面 HB 等工艺还需背面金属与穿硅接入模型；本版不把这类结构当作已支持的直接 HB。背景参考：[imec 的正背面晶圆连接说明](https://www.imec-int.com/en/articles/path-high-density-front-and-backside-wafer-connectivity)。

Web 在应用、校验、评估及寻优入口检查朝向冲突、直接 HB 接触面不匹配和非相邻接口；未指定朝向或接口计为待补数据。在「实现难点」与「约束检查」中展示 `STACK_GEOMETRY`。这些附加检查由 Web 从快照重新生成，独立运行旧版引擎及旧版导出 HTML 不包含它们；已有数值资源计算保持原引擎口径，未新增 RC、背面布线或签核级计算。

## 验证

`node --test tests/*.test.mjs` 覆盖四种接触关系、旧输入推断、冲突拦截、朝向/接口原子更新、独立 YAML 往返、Die 改名引用同步。

真实 BLX 输入通过本地预览 API 往返后，536 个模块的放置数据不变；信号 TSV 1,182,400、HB 715,200 bit 不变。测试仅预览，不写入示例工程或历史运行。
