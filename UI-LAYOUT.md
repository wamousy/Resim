# Resim 页面组织

页面以两项任务为中心：识别当前架构实现难点，辅助 partition 和 floorplan 规划。

## 工作区

| 页面 | 使用目的 |
| --- | --- |
| 架构输入 | 工程创建、导入 YAML、输出目录、Die 参数和输入校验 |
| 布局规划 | 整体/单核、层视图、模块选择、坐标编辑与跨层移动 |
| 实现难点 | 分开呈现违例、风险和待补数据，查看对象与建议，返回布局定位 |
| 连接与布线 | 检索连接、查看位宽、带宽和金属资源 |
| 资源与计算 | 逐层资源账本、工艺参数、公式与计算依据 |
| 结果管理 | 历史运行、结果目录、报告导出与候选比较 |

固定导航维持页面切换；顶部保留工程、当前方案、自动规划和评估保存。布局页的模型占据主区域，右侧显示选中对象与约束检查。模块编辑、显示选项、尺寸明细按需展开。专注视图隐藏外围控件，Esc 返回。

网页切换不提交输入、不重新模拟，也不丢弃当前草稿。资源计算和输入格式没有因本次界面调整而改变。

## 当前前端文件

本仓库当前使用发布包目录 `_internal/resim/static/` 中的前端文件。`index.html` 定义页面，`studio.css` 定义工作台布局，`workbench.js` 负责导航，`app.js` 连接现有评估数据。运行中的程序直接提供这些静态文件，刷新页面即可载入更新。
